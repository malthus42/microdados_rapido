[project]
name = "microdados_rapido"
version = "0.1.0"
description = "Uma biblioteca para processar microdados do ENEM"
readme = "README.md"
requires-python = ">=3.8"
dependencies = [
    "duckdb",
    "pandas",
    "pyarrow",
]

[tool.setuptools.packages.find]
where = ["."]